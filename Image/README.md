# `Image/` — image/charting library (bundled)

This folder contains PHP image/charting related code used for graphing and visualization features.

You may see subfolders such as:
- `Canvas/`
- `Graph/`

Notes:
- Treat as bundled/library code unless you are explicitly patching it.
- Related entrypoint sometimes used for graph output: `graph.php` (depending on feature usage).
