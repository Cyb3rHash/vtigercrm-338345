# `log4php.debug/` — bundled Log4PHP source tree (debug)

This directory contains a more complete Log4PHP source tree and related configuration/layout examples.

## Notes
- Treat this as **vendor/bundled code**.
- vtiger’s main runtime integration points are documented under `log4php/` and `log4php.properties`.
