# `modules/Settings/MailScanner/` — Mail Scanner configuration (admin)

MailScanner is used to configure inbound email processing (connecting to mailboxes, scanning emails, mapping them to CRM records, etc.).

What you’ll find here:
- Admin configuration screens and actions
- `core/` — core mail scanner implementation logic used by the settings UI

Related:
- `modules/Emails/` (email handling)
- `modules/Settings/README.md`
