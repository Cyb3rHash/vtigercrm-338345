# `modules/Settings/MailScanner/core/` — MailScanner core logic

This directory contains the underlying implementation used by the MailScanner settings pages.

Notes:
- Typically invoked/configured by UI actions in `modules/Settings/MailScanner/`.
- If debugging “mail not getting scanned/imported”, start here and follow call sites from the Settings UI.
