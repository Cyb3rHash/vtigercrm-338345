# Faq module (`modules/Faq/`)

The **Faq** module manages FAQ/knowledge base entries.

## Where to start
- Entity/model: `Faq.php` (extends `data/CRMEntity.php`)
- Common UI actions:
  - `ListView.php`, `DetailView.php`, `EditView.php`, `Save.php`
  - AJAX entrypoints (`*Ajax*.php`)
