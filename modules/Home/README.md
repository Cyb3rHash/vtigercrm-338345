# Home module (`modules/Home/`)

Implements the vtiger home page experience (widgets, shortcuts, home-level actions).

What you’ll find:
- Home UI scripts and AJAX endpoints
- `js/` client-side scripts for home widgets

Notes:
- UI composition module (not CRMEntity-based).
