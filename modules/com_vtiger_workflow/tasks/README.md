# `modules/com_vtiger_workflow/tasks/` — workflow task implementations

This folder contains workflow task classes/forms that define actions executed when a workflow triggers (e.g., send email, update field, create record).

Related:
- `modules/com_vtiger_workflow/VTTaskManager.inc`
- Workflow UI: `modules/com_vtiger_workflow/editworkflow.php`, `edittask.php`
