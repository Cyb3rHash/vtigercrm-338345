# `modules/com_vtiger_workflow/resources/` — workflow UI/resources

This folder contains workflow-related static resources used by the workflow module (JS/CSS/templates depending on module needs).

Related:
- `Smarty/templates/com_vtiger_workflow/`
