# `modules/Migration/DBChanges/` — database change scripts (upgrades)

This folder contains migration/upgrade SQL/PHP scripts applied when upgrading vtiger versions.

Notes:
- Used by migration flows in `modules/Migration/`.
- If you are troubleshooting an upgrade, identify the target version and inspect the relevant change scripts here.
