# `storage/` — storage marker / placeholder

This folder is used as a marker/placeholder in this repository layout.

Current content:
- `vtiger.txt` — indicates this is a vtiger storage-related area.

## Typical vtiger deployments
In production, vtiger stores uploaded attachments in a configured directory (commonly `storage/` or `test/` or a custom path depending on configuration). This repo includes the directory for completeness, but actual attachment storage location depends on configuration and deployment.
