# `include/Zend/` — Zend Framework components (bundled)

This directory contains selected Zend Framework components used by vtiger.

Common usage:
- JSON support used by the webservice layer.

Related:
- `webservice.php`
- `include/Webservices/*`
