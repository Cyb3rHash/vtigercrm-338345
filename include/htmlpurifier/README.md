# `include/htmlpurifier/` — HTMLPurifier (third-party)

HTMLPurifier is a security library used to sanitize HTML content to prevent XSS and unsafe markup.

Notes:
- Treat as vendor code.
- Cache/config may exist under `test/vtlib/HTML/` in this repo (see `test/README.md`).
