# `include/fpdf/` — FPDF (third-party PDF generation)

This folder contains the FPDF library and related resources.

Notes:
- Treat as vendor code.
- vtiger may use FPDF for some legacy PDF outputs; TCPDF is also bundled (`include/tcpdf/`).
