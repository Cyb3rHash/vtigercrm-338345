# `include/ckeditor/` — CKEditor (third-party)

CKEditor provides rich text editing in vtiger UI (email templates, descriptions, notes, etc.).

Notes:
- Treat as vendor code.
- Configuration/integration is done by vtiger UI scripts and templates, not by editing CKEditor source directly (unless patching).
