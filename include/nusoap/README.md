# `include/nusoap/` — NuSOAP (third-party)

NuSOAP is used by vtiger’s SOAP service implementations.

Related:
- SOAP dispatcher: `vtigerservice.php`
- SOAP services: `soap/` (see `soap/README.md`)

Notes:
- Treat as vendor code unless patching intentionally.
