# `jscalendar/` — JavaScript calendar widget (third-party)

This directory contains a bundled JavaScript calendar/date-picker library used by vtiger’s UI.

Contents include:
- core JS/CSS
- `lang/` — localization resources

Notes:
- Treat as **vendor code**.
- Used by date fields across multiple modules and forms.
